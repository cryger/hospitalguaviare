import { ReadOnHoverDirective } from './read-on-hover.directive';

describe('ReadOnHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new ReadOnHoverDirective();
    expect(directive).toBeTruthy();
  });
});
